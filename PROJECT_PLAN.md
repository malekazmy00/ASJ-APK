# ASJ Medical Systems Store — خطة إعادة البناء بـ Flutter

## 1. نظرة عامة
إعادة بناء نظام "ASJ Medical Systems Store" (حالياً Streamlit) كتطبيق Flutter (APK) بنفس وظائف النظام الحالي، بشكل تطبيق احترافي حقيقي، مع ميزتين جديدتين (تتبع القطعة عبر الزمن، تتبع جلسات وأعمال المستخدمين)، وتأجيل ميزة رفع الصور لمرحلة لاحقة.

**قرارات معمارية أساسية (مؤكدة سابقاً):**
- لا يوجد backend server منفصل. التطبيق يتصل بـ Supabase مباشرة (REST API التلقائي + Edge Functions للمنطق الحساس مثل Gemini).
- البناء (APK) عبر Codemagic (CI سحابي) بسبب عدم وجود بيئة تطوير محلية.
- إدارة الكود عبر GitHub (نفس الريبو الحالي أو ريبو جديد `asj-flutter`).

## 2. المكدس التقني (Tech Stack)
| الطبقة | الاختيار | السبب |
|---|---|---|
| اللغة/الإطار | Flutter (Dart) | مطلوب صراحة، APK مباشر |
| إدارة الحالة | Riverpod | قابلية اختبار، منتشر، مناسب لمشروع متوسط الحجم بأدوار متعددة |
| التوجيه | go_router | دعم أدوار/صلاحيات (redirect) ومسارات معلّمة |
| الاتصال بـ Supabase | supabase_flutter (رسمي) | REST + Auth + Storage (لاحقاً) + Realtime |
| الشكل/التصميم | Material 3 مخصص (ألوان وخطوط ASJ) | هوية بصرية حقيقية بدل الشكل الافتراضي |
| البناء السحابي | Codemagic | فري تير كافٍ لمرحلة التطوير |

## 3. هيكل المشروع (Feature-First)
```
lib/
  core/
    config/        -> مفاتيح Supabase، ثوابت عامة
    theme/          -> ألوان، خطوط، AppTheme
    router/         -> go_router + حراسة الأدوار (role guards)
    models/         -> AppUser, InventoryItem, TransactionLog ...
    widgets/         -> عناصر مشتركة (TimelineWidget, StatCard ...)
  features/
    auth/            -> تسجيل الدخول، إدارة الجلسة
    home/             -> توجيه حسب الدور بعد الدخول
    worker/           -> تحليل AI (بدون صور حالياً) -> تعديل -> حفظ
    engineer/         -> بحث ذكي، صرف/بيع، لوحة تعديل، قاعدة معرفة
    admin/            -> مستخدمين، صلاحيات، استيراد قاعدة معرفة، إشعارات
    item_timeline/    -> [جديد] تتبع القطعة عبر الزمن
    user_activity/    -> [جديد] تتبع جلسات ونشاط المستخدمين
    inventory_summary/-> [جديد] الداشبورد التجميعي (Group by Part Number)
```
كل الميزات الثلاث الجديدة (item_timeline / user_activity / inventory_summary) ستُبنى فوق **Widget واحد مشترك قابل لإعادة الاستخدام** (`TimelineWidget` في `core/widgets`)، لأن الثلاثة أنماط عرض متشابهة (جدول/تايم لاين زمني قابل للفلترة والتجميع) على مصادر بيانات مختلفة.

## 4. تعديلات قاعدة البيانات المطلوبة (Supabase)
1. `serial_number` على `inventory_items` — معلّق من قبل، يُنفَّذ الآن.
2. جدول جديد `user_sessions`:
   - `id`, `user_id` (FK -> users), `login_at`, `last_activity_at`, `logout_at` (nullable), `device_info` (اختياري)
   - منطق "نهاية الجلسة": إما تسجيل خروج صريح، أو timeout بعد فترة عدم نشاط (يُحسب من `last_activity_at`).
3. View أو Query مجمّع لـ `inventory_items`:
   - `SELECT part_number, COUNT(*) as qty FROM inventory_items GROUP BY part_number` كأساس للداشبورد التجميعي.

## 5. مراحل التنفيذ
| المرحلة | المحتوى | الحالة |
|---|---|---|
| 0 | خطة + هيكل معماري | ✅ |
| 1 | الأساس: اتصال Supabase + تسجيل دخول + توجيه حسب الدور | ✅ |
| 2 | شاشات الأدوار الثلاثة | 🟡 الأساسي جاهز + دفتر الاستعلامات، ناقص جزئياً (راجع القسم 7.1) |
| 3 | الميزات الجديدة (تتبع القطعة، تتبع المستخدمين، الداشبورد التجميعي) | ✅ (تحتاج اختبار حي + تشغيل SQL migration) |
| 4 | Edge Functions (Gemini، منطق قاعدة المعرفة) | ✅ (login-user، analyze-part، admin-create-user — غير مختبرة حياً بعد) |
| 5 | رفع الصور (تحليل AI بالصورة ✅ — الحفظ الدائم على Storage لسه مؤجل) | 🟡 جزئي — راجع القسم 7.2 |
| 6 | تجهيز CI على Codemagic وأول APK فعلي | 🟡 الإعداد جاهز (`codemagic.yaml` بيولّد android/ تلقائياً) — لم يُختبر حياً |

## 7. قرار تم اتخاذه أثناء التنفيذ (يستحق الانتباه)
نظام الدخول اتبنى على الإبقاء بجدول `users` ونظام Argon2 الحالي (مش الانتقال لـ Supabase Auth)، عشان محدش من المستخدمين الحاليين يحتاج تسجيل جديد. التحقق من كلمة المرور بقى عبر Edge Function اسمها `login-user` بدل الاعتماد على `signInWithPassword`. تفاصيل الأمان والمقايضة موثقة كتعليق داخل `auth_service.dart`.

### 7.1 الناقص فعلياً في المرحلة 2 (بصراحة كاملة)
تم بناء التدفقات الأساسية والأكثر استخداماً لكل دور فقط، وليس كل شاشة موجودة في نسخة Streamlit حرفياً. الباقي:
- **العامل**: مكتمل (إدخال يدوي بدل الصورة، تحليل AI، حفظ جماعي بالكمية).
- **المهندس**: البحث الذكي + الصرف + لوحة التعديل جاهزين. **ناقص**: تبويب "دفتر الاستعلامات" (طلب قطعة غير متوفرة مع السبب/التاجر - جدول `engineer_queries`)، وتبويب تعديل قاعدة المعرفة المباشر (منفصل عن نتيجة البحث).
- **الأدمن**: إدارة المستخدمين + الإشعارات جاهزين. **ناقص**: استيراد قاعدة المعرفة من CSV، والإحصائيات/الإعدادات.
هذا قرار أولوية (الأهم أولاً) مش سهو — نكمل الباقي في تكملة المرحلة 2 لو موافق.

### 7.2 توضيح قرار الصورة (تم تعديله بناءً على توجيه صريح)
القرار الأصلي كان تأجيل "رفع الصور" بالكامل. تم التصحيح: **التقاط/اختيار الصورة واستخدامها في تحليل الذكاء الاصطناعي (worker) مُفعّل وشغال الآن** (عبر `image_picker`، وإرسالها base64 لدالة `analyze-part` بالضبط زي النظام الأصلي). **المؤجل فعلياً** هو خطوة واحدة بس: حفظ الصورة نفسها بشكل دائم على Supabase Storage بعد التحليل (بحيث تظهر لاحقاً في تفاصيل القطعة). لا يوجد `image_path` بيتسجل في `inventory_items` حالياً.

## 6. ملاحظات مهمة منقولة من النظام الحالي (لتفادي تكرار نفس الأخطاء)
- منطق تصعيد نماذج Gemini (lite→flash→pro) ومنطق دمج قاعدة المعرفة (عدم الكتابة فوق بيانات موثوقة) يُنقلان كما هما إلى Edge Function، وليس إلى كود Dart، لأنهما يحتاجان مفتاح Gemini السري.
- عدم تكرار خطأ رفع الصور: عند تنفيذ المرحلة 5 لاحقاً، الرفع يجب أن يُختبر مباشرة على Supabase Storage (وليس مسار محلي) قبل اعتباره مكتملاً.
- صلاحيات can_export/can_track/can_edit تُطبَّق كـ Row Level Security (RLS) على مستوى Supabase قدر الإمكان، بدل الاعتماد فقط على منطق واجهة التطبيق (أكثر أماناً لتطبيق موبايل).
